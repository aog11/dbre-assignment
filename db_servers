# Invetory file, using the public IP of the VMs to reach them
# The values set here come from the output of the VMs provisioning with Terraform:
# master  = machine_public_ip[0]
# replica = machine_public_ip[1]

[master]
34.138.183.77

[replica]
34.148.201.187
